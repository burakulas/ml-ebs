# ML-EBS: Machine Learning for Eclipsing Binary Stars
# Requirements file for Python dependencies

# Core scientific computing
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0

# Machine learning
scikit-learn>=1.0.0
xgboost>=1.5.0

# Visualization
matplotlib>=3.4.0

# Utilities
tqdm>=4.62.0

# Optional: GPU acceleration for predictions (comment out if not using GPU)
# Uncomment the line below and replace with your CUDA version
# cupy-cuda11x>=10.0.0  # Replace 11x with your CUDA version (e.g., cupy-cuda117)

# Note: For CPU-only usage, CuPy is not required. The prediction scripts
# will automatically fall back to NumPy if CuPy is not available.
